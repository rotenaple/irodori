/**
 * Constants shared across image processing modules
 */

// Maximum number of palette colors supported
export const MAX_PALETTE_SIZE = 256;

// Maximum pixels for workspace (to prevent memory issues)
export const MAX_WORKSPACE_PIXELS = 10000000;

// Target file size for NationStates compatibility (bytes)
export const NS_TARGET_SIZE = 150 * 1024;
